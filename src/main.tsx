import React from "react";
import ReactDOM from "react-dom/client";

import GiggifiWebsite from "./GiggifiWebsite";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <GiggifiWebsite />
  </React.StrictMode>,
);
